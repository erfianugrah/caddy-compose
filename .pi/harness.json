{
  "task": "Update AGENTS.md (this repo's agent-facing operational doc) to match the current edge reality. README.md and PLAN.md are already updated - follow them. Required changes: (1) Project Overview: remove Authelia from the stack description (retired 2026-07, no IdP in the stack); the dashboard is Astro 7 (not Astro 6). (2) All deploy/ops content must target the edge deployment: the stack lives on the MS-01 NixOS router (ssh alias router) as the composer stack 'edge-services'; the LIVE Caddyfile is deploy/edge/Caddyfile and the repo-root Caddyfile is legacy servarr config; caddy ops recipes must use 'ssh router', never 'ssh servarr'. (3) Replace every ~/knot-fly reference: the Knot app merged into the knotea monorepo - ~/knot-fly/AGENTS.md becomes ~/knotea/authority/AGENTS.md and ~/knot-fly/docs/runbooks/cf-to-knot-migration.md becomes ~/knotea/authority/docs/runbooks/cf-to-knot-migration.md (gotcha #25 cross-references become 'the knot-dns skill / knotea authority AGENTS.md'). (4) The Edge HTTP cache bullet must state the cache was REMOVED from the running config 2026-08-07 (commit 02b0706), modules still compiled into the image, quirks preserved in test/cache/README.md, and that tools/cachectl verify has a stale assert. (5) Add the 2026-08-09 direction change as a prominent note near the top: CRS/WAF slated for removal, wafctl being renamed edgectl - see PLAN.md 'Direction Change'. Document the system AS DEPLOYED TODAY; the nuke is planned, not executed. (6) Fix the stale cross-reference ~/servarr-compose/AGENTS.md to ~/infra/servarr-compose/AGENTS.md (keep the curl-UA gotcha itself - still true). (7) Document the wafctl -> Caddy admin routing: wafctl never talks to :2019 directly, it goes through the IP-restricted admin proxy on :2020 (extra_hosts: caddy:<bridge-gateway> is load-bearing). (8) Update version pins ONLY where verifiable in Makefile/Dockerfile (caddy image 3.97.1-2.11.4 per Makefile, wafctl 2.101.3, body-matcher v0.2.1, policy-engine v0.42.2, ddos-mitigator v0.17.3, caddy-l4 v0.1.2); keep test counts approximate. Do NOT delete durable operational knowledge: the SOPS re-encrypt / make restart vs restart-caddy gotcha, the three-edit rule for env vars, the /load fingerprint trick, TSIG rotation order, the composer curl-UA gotcha, the 7-pass pipeline and CRS converter descriptions (still the running system), the zone-migration pattern.",
  "maxIterations": 5,
  "models": ["openrouter/deepseek/deepseek-v4-pro"],
  "stallPatience": 2,
  "tools": ["read", "edit", "write", "bash", "grep"],
  "writeScope": ["AGENTS.md"],
  "guide": [
    "README.md",
    "PLAN.md",
    "/home/erfi/.pi/agent/skills/caddy/SKILL.md"
  ],
  "rules": [
    "Only AGENTS.md may be modified; do not touch any other file.",
    "Do not run git commit, git reset, git checkout, or git stash - leave git refs alone.",
    "Preserve durable operational knowledge; this is an update, not a rewrite. Match the existing file's tone and density.",
    "ASCII punctuation only in your edits: hyphens not em-dashes, straight quotes, no ellipsis character.",
    "Do not invent version numbers or test counts. If you cannot verify a number from Makefile/Dockerfile/package.json in this repo, keep it approximate.",
    "The WAF nuke and edgectl rename are PLANNED, not done: describe them as direction/next-steps, never as current state."
  ],
  "sensors": [
    { "name": "keep-sops-gotcha",
      "cmd": "rg -q 'restart-caddy' AGENTS.md",
      "hint": "the make restart vs restart-caddy SOPS footgun gotcha must be preserved" },
    { "name": "keep-deploy-pipeline",
      "cmd": "rg -q 'policy-rules.json' AGENTS.md",
      "hint": "the deploy pipeline architecture note (policy-rules.json mtime hot-reload) must be preserved" },
    { "name": "keeps-wafctl-name",
      "cmd": "rg -q 'wafctl' AGENTS.md",
      "hint": "the rename to edgectl is planned, not executed - the doc must still describe wafctl as the running system, with the rename as a forward note" },
    { "name": "fences-balanced",
      "cmd": "test $(( $(rg -c '^```' AGENTS.md) % 2 )) -eq 0",
      "hint": "unbalanced code fences - a code block was left open" },
    { "name": "rename-noted", "expect": "fail",
      "cmd": "rg -q 'edgectl' AGENTS.md",
      "hint": "add the 2026-08-09 direction-change note: wafctl being renamed edgectl, CRS/WAF slated for removal, see PLAN.md Direction Change" },
    { "name": "no-knot-fly", "expect": "fail",
      "cmd": "! rg -q 'knot-fly' AGENTS.md",
      "hint": "knot-fly merged into the knotea monorepo: ~/knot-fly/AGENTS.md -> ~/knotea/authority/AGENTS.md, runbooks under ~/knotea/authority/docs/runbooks/" },
    { "name": "no-astro-6", "expect": "fail",
      "cmd": "! rg -q 'Astro 6' AGENTS.md",
      "hint": "dashboard is Astro 7 now (README.md says so)" },
    { "name": "cache-removal-noted", "expect": "fail",
      "cmd": "rg -qi 'cache removed|removed from the running config' AGENTS.md",
      "hint": "the Edge HTTP cache bullet must say the cache was removed from the running config 2026-08-07 (commit 02b0706), modules still in the image, quirks in test/cache/README.md" },
    { "name": "edge-stack-named", "expect": "fail",
      "cmd": "rg -q 'edge-services' AGENTS.md",
      "hint": "the deploy story must name the composer stack 'edge-services' on the MS-01 (ssh router)" },
    { "name": "admin-proxy-documented", "expect": "fail",
      "cmd": "rg -q ':2020' AGENTS.md",
      "hint": "document wafctl -> Caddy admin routing via the IP-restricted admin proxy on :2020 (never :2019 directly)" },
    { "name": "no-ssh-servarr", "expect": "fail",
      "cmd": "! rg -q 'ssh servarr' AGENTS.md",
      "hint": "caddy ops recipes target the edge router: ssh router, not ssh servarr" }
  ]
}
